version: 2
jobs:
  build:

    working_directory: ~/src

    docker:
      - image: circleci/openjdk:8-jdk-browsers

    steps:

      - checkout

      - run: mvn clean verify

      - store_test_results:
          path: CalculatorTest/src/test/java/

      